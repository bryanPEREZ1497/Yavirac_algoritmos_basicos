const array1 = ['a', 'b', 'c'];

array1.forEach((element, index) => {
    console.log(`
        El array ${array1}, el elemento cuyo índice es ${index} es ${element}
`)
});

const hola = 'hola'; 
const hola1 = 'hola'

